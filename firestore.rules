rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // הרשאות פתוחות זמנית - כל המשתמשים המחוברים יכולים לקרוא ולכתוב הכל
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
